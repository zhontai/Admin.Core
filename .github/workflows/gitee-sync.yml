name: Publish 
on:
    push:
        branches:
            - master
            - dev
            
jobs:
    build:
        runs-on: ubuntu-latest
        steps:
            - name: Sync to Gitee 💕
              uses: TTonlyV5/git-mirror-action@master
              env:
                  SSH_PRIVATE_KEY: ${{ secrets.SSH_PRIVATE_KEY_HOME }}
              with:
                  source-repo: "git@github.com:TTonlyV5/Admin.Core.git"
                  destination-repo: "git@gitee.com:TTonlyV5/Admin.Core.git"
